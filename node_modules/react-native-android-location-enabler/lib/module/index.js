import { NativeModules, Platform } from 'react-native';
const LINKING_ERROR = `The package 'react-native-android-location-enabler' doesn't seem to be linked. Make sure: \n\n` + Platform.select({
  ios: "- You have run 'pod install'\n",
  default: ''
}) + '- You rebuilt the app after installing the package\n' + '- You are not using Expo Go\n';
const AndroidLocationEnabler = NativeModules.AndroidLocationEnabler ? NativeModules.AndroidLocationEnabler : new Proxy({}, {
  get() {
    throw new Error(LINKING_ERROR);
  }
});
export function promptForEnableLocationIfNeeded(options) {
  return AndroidLocationEnabler.promptForEnableLocationIfNeeded(options);
}
export function isLocationEnabled() {
  return AndroidLocationEnabler.isLocationEnabled();
}
//# sourceMappingURL=index.js.map